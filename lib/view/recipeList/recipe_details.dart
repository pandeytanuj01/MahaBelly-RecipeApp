import 'package:flutter/material.dart';

class RecipeDetails extends StatelessWidget {
  final dynamic data;

  RecipeDetails({this.data});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
